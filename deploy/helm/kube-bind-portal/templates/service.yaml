apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-bind-portal.fullname" . }}
  labels:
    {{- include "kube-bind-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-bind-portal.selectorLabels" . | nindent 4 }}
